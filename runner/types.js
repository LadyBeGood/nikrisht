/**
 * @typedef { "red" | "yellow" | "bold" | "dim" | "blue" } CurrentlyRequiredColors
 * @typedef {((text: string) => string)} ColorFunction
 */


/**
 * @typedef {Record<CurrentlyRequiredColors, ColorFunction>}  Colors
 */